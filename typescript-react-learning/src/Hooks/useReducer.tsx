import React, { useReducer } from "react";

type ACTIONTYPE =
  | {
      type: "increment";
      payload: number;
    }
  | {
      type: "decrement";
      payload: string;
    };

function reducer(state: typeof )

interface IUser {}

const App = () => {};

export default App;
